Generating the docs
----------

We use [mkdocs](http://www.mkdocs.org/) structure for the documentation. 

Build locally with:

```bash 
mkdocs build
```

Serve locally with:
```bash
  mkdocs serve
```